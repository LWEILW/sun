<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blogger.dao.PermissionMapper.PermissionMapperEx">

    <select id="findPermissionByUid" parameterType="java.lang.Integer" resultType="com.blogger.entity.PermissionEntity.Permission">
        select * from u_permission where id=#{id}
    </select>

</mapper>
